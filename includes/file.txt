**You are a senior PHP developer helping me build a complete website called "Study Planner" from scratch.**
> 
> 
> **Tech Stack:**
> 
> 
> 
> - Backend: PHP (procedural, no frameworks)
> - Database: MySQL (via mysqli)
> - Frontend: HTML + CSS + vanilla JavaScript (no frameworks)
> - Server: XAMPP (localhost)
> - All files must work at: `http://localhost/study_planner/`
> 
> **Project Folder Structure:**
> 
> 
> 
> 
> 
> ```
> study_planner/
> ├── index.php
> ├── study_planner.sql
> ├── css/
> │   └── style.css
> ├── includes/
> │   ├── db.php
> │   ├── auth.php
> │   └── layout.php
> └── pages/
> ├── register.php
> ├── login.php
> ├── logout.php
> ├── forgot_password.php
> ├── reset_password.php
> ├── subjects.php
> ├── sessions.php
> ├── dashboard.php
> ├── todos.php
> ├── notes.php
> ├── calendar.php
> ├── history.php
> └── admin/
> └── index.php
> ```
> 
> 
> 
> **Database: `study_planner.sql` must create these tables:**
> 
> 
> 
> - `users` — id, username (unique), email (unique), password_hash, role (student/admin), created_at, last_login
> - `password_resets` — id, user_id (FK), token, expires_at, used, created_at
> - `subjects` — id, user_id (FK), name, color (hex), created_at. UNIQUE(user_id, name)
> - `study_sessions` — id, user_id (FK), subject_id (FK), session_date, duration_min, notes, created_at
> - `weekly_goals` — id, user_id (FK), subject_id (FK), goal_hours, week_start. UNIQUE(user_id, subject_id, week_start)
> - `todos` — id, user_id (FK), subject_id (FK nullable), task, is_done, created_at
> - `quick_notes` — id, user_id (FK), subject_id (FK nullable), content, updated_at. UNIQUE(user_id, subject_id)
> 
> **Complete Feature List (all must be built):**
> 
> 
> **Sprint 1 – Authentication:**
> 
> 
> 
> - US001: Register with username, email, password. Validate: unique email/username, min 8 chars password with 1 uppercase + 1 number, confirm password match.
> - US002: Login with email + password. Show generic error (don't reveal which field is wrong). Update last_login on success. Logout clears session.
> - US003: Forgot password generates a token (stored in DB, expires 1 hour). On localhost, show the reset link directly on screen instead of email. Reset password page validates token, lets user set new password, marks token used.
> 
> **Sprint 2 – Subjects & Sessions:**
> 
> 
> 
> - US004: Add, edit, delete subjects. Each subject has a name and a color (picker with preset hex colors). Cannot delete a subject that has sessions linked to it — show an error instead.
> - US005: Log a study session: choose subject, pick date (not future), enter duration in minutes (must be > 0), optional notes.
> - US006: Edit or delete any of your own sessions. Changes reflect immediately.
> 
> **Sprint 3 – Dashboard & Goals:**
> 
> 
> 
> - US007: Dashboard shows: today's total study time, this week's total, total session count (all time), streak counter.
> - US008: Set a weekly goal (in hours) per subject, stored in weekly_goals table with week_start = Monday of current week.
> - US009: Progress bars per subject showing (actual minutes this week / goal minutes) as a percentage. Bar turns green when 100% reached.
> 
> **Sprint 4 – Productivity:**
> 
> 
> 
> - US010: To-do list. Add tasks with optional subject link. Filter by subject. Toggle done/undone with checkbox (auto-submits form). Delete task. Completed tasks show strikethrough.
> - US011: Study streak counter on dashboard — counts consecutive days (today backwards) where at least 1 session was logged. Resets if a day is missed.
> - US012: Quick notes — one note per subject, max 2000 characters, auto-save on submit, shows last saved timestamp. Character counter updates live with JavaScript.
> 
> **Sprint 5 – History, Calendar & Admin:**
> 
> 
> 
> - US013: Calendar view — monthly grid (Mon–Sun), days with sessions are highlighted. Click a day to see session details below the calendar. Prev/Next month navigation via GET params.
> - US014: History/report page — filter sessions by date range and subject. Show total sessions count, total time, average per session as stat cards above the table.
> - US015: Admin panel (role = admin only). Shows platform stats (total users, sessions, subjects) and a full user table: username, email, role, subject count, session count, registered date, last login.
> 
> **Security requirements (must implement all):**
> 
> 
> 
> - CSRF token on every POST form
> - Passwords hashed with password_hash (BCRYPT, cost 12)
> - All user input sanitized with htmlspecialchars() before output
> - All DB queries use prepared statements with bind_param
> - requireLogin() redirects to login if not logged in
> - requireAdmin() redirects to dashboard if not admin
> - Users can only see/edit their own data (always filter by user_id in queries)
> 
> **Design requirements:**
> 
> 
> 
> - Dark theme: navy background (`#0F1B2D`), indigo accent (`#4F6EF7`), teal highlights (`#2DD4BF`)
> - Font: Inter (body) + Space Grotesk (headings) from Google Fonts
> - Sticky navbar with all page links. Show username when logged in.
> - Responsive: works on mobile (grid collapses to 1 column below 500px)
> - Flash messages styled in green (success), red (error), blue (info)
> - Modals (pure CSS/JS, no libraries) for add/edit forms on subjects and sessions pages
> - Progress bars, badge pills, stat cards — all styled in CSS variables
> 
> **Code style:**
> 
> 
> 
> - Procedural PHP, no classes or frameworks
> - Every file starts with require_once for db.php, auth.php, layout.php
> - layoutHeader('Page Title') and layoutFooter() wrap every page
> - db.php uses a static variable so the connection is made only once
> - No inline styles except for dynamic values like subject colors
> - All paths use /study_planner/ as the base (works on XAMPP htdocs)
> 
> **Delivery format:**
> 
> 
> 
> - Give me one file at a time
> - Start each file with a comment showing: filename and its destination path
> - After each file, wait for me to confirm before giving the next one
> - At the end, give me the full folder structure summary
> - Start with study_planner.sql, then includes/ files, then css/style.css, then pages one by one